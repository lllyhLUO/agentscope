---
phase: 02-publish-workflow
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/agentscope/skill/__init__.py
  - src/agentscope/skill/_cli.py
  - src/agentscope/skill/_publisher.py
  - src/agentscope/skill/_repository.py
  - tests/skill_registry_publish_command_test.py
autonomous: true
requirements: [PUB-01, PUB-03]
user_setup:
  - service: postgresql
    why: "Real publish execution needs a configured registry database URL and, optionally, a maintainer principal/test database"
    env_vars:
      - name: AGENTSCOPE_SKILL_REGISTRY_DATABASE_URL
        source: "Developer-provided write-capable PostgreSQL URL for the skill registry"
      - name: AGENTSCOPE_SKILL_REGISTRY_TEST_DATABASE_URL
        source: "Optional isolated PostgreSQL URL for publish smoke tests"
must_haves:
  truths:
    - "D-06/D-07: Publish fails when the same `skill_name@version` already exists with different content and may return idempotent success when content matches"
    - "D-08: Publisher enforces maintainer write authorization before inserting new registry rows"
    - "D-09: Maintainers have a minimal command path to publish a local skill directory by explicit `skill_name` and `version`"
  artifacts:
    - path: "src/agentscope/skill/_publisher.py"
      provides: "Database-backed publish service built on the validated manifest"
      contains: "publish_skill_directory"
    - path: "src/agentscope/skill/_cli.py"
      provides: "Minimal publish command path"
      contains: "publish"
    - path: "tests/skill_registry_publish_command_test.py"
      provides: "Publish-service and command-path coverage"
      contains: "class SkillRegistryPublishCommandTest"
  key_links:
    - from: "src/agentscope/skill/_publisher.py"
      to: "src/agentscope/skill/_repository.py"
      via: "maintainer access and registry persistence"
      pattern: "assert_can_write"
    - from: "src/agentscope/skill/_cli.py"
      to: "src/agentscope/skill/_publisher.py"
      via: "publish command dispatch"
      pattern: "publish_skill_directory"
---

<objective>
Implement the database-backed publish path for registry skills.

Purpose: Turn a validated local skill directory manifest into an immutable
registry version in PostgreSQL and expose a minimal command path maintainers can
use now.
Output: Publisher write logic, minimal publish command path, repository write
helpers as needed, and tests covering publish success, conflict rejection, and
idempotent re-publish behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-publish-workflow/02-CONTEXT.md
@.planning/phases/01-registry-foundation/01-02-SUMMARY.md
@docs/superpowers/specs/2026-04-28-skill-registry-design.md
@src/agentscope/skill/_publisher.py
@src/agentscope/skill/_repository.py
@src/agentscope/skill/_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing tests for publish success, conflict rejection, and command dispatch</name>
  <files>tests/skill_registry_publish_command_test.py</files>
  <read_first>tests/skill_registry_publish_command_test.py, tests/skill_registry_repository_test.py, tests/skill_registry_publisher_test.py, src/agentscope/skill/_repository.py, src/agentscope/skill/_publisher.py</read_first>
  <action>Create `tests/skill_registry_publish_command_test.py` with isolated tests for the database-backed publish workflow. Cover: first publish creates a skill/version/file record set, second publish of the same `skill_name@version` with different content raises an error, second publish with identical content returns an idempotent success result, maintainer checks are enforced before writes, and the minimal publish command path routes `publish` arguments into the publisher service. Keep tests driver-independent using repository/session doubles or env-gated PostgreSQL smoke checks rather than depending on a local async SQLite driver.</action>
  <verify>PYTHONPATH=src pytest tests/skill_registry_publish_command_test.py</verify>
  <acceptance_criteria>
    - tests/skill_registry_publish_command_test.py contains "class SkillRegistryPublishCommandTest"
    - tests/skill_registry_publish_command_test.py contains "different content"
    - tests/skill_registry_publish_command_test.py contains "identical content"
    - tests/skill_registry_publish_command_test.py contains "publish"
  </acceptance_criteria>
  <done>Publish workflow expectations are captured in failing tests</done>
</task>

<task type="auto">
  <name>Task 2: Implement publish-to-registry behavior on top of repository and manifest helpers</name>
  <files>src/agentscope/skill/_publisher.py, src/agentscope/skill/_repository.py, src/agentscope/skill/__init__.py</files>
  <read_first>src/agentscope/skill/_publisher.py, src/agentscope/skill/_repository.py, src/agentscope/skill/_models.py, src/agentscope/skill/__init__.py, .planning/phases/02-publish-workflow/02-CONTEXT.md, docs/superpowers/specs/2026-04-28-skill-registry-design.md</read_first>
  <action>Add a publish service to `_publisher.py`, exposed as `publish_skill_directory(...)`, that accepts at minimum a local directory path, explicit `skill_name`, explicit `version`, repository instance, and maintainer principal. Reuse the manifest builder from Plan 02-01, call `repository.assert_can_write(...)` before mutation, look up existing `skill_name@version`, raise on conflicting content, return idempotent success on identical content, and insert new skill/version/file rows when publishing a new artifact. Extend `_repository.py` only with the write helpers needed to keep database writes inside the repository boundary, and update `src/agentscope/skill/__init__.py` to expose the new publish entrypoint.</action>
  <verify>rg -n 'publish_skill_directory|assert_can_write|different content|identical content' src/agentscope/skill/_publisher.py src/agentscope/skill/_repository.py src/agentscope/skill/__init__.py</verify>
  <acceptance_criteria>
    - src/agentscope/skill/_publisher.py contains "publish_skill_directory"
    - src/agentscope/skill/_publisher.py contains "different content"
    - src/agentscope/skill/_publisher.py contains "identical content"
    - src/agentscope/skill/_publisher.py contains "assert_can_write"
    - src/agentscope/skill/__init__.py contains "publish_skill_directory"
  </acceptance_criteria>
  <done>Publisher can persist new versions, reject conflicts, and return idempotent success for identical re-publish</done>
</task>

<task type="auto">
  <name>Task 3: Add a minimal publish command path and turn publish tests green</name>
  <files>src/agentscope/skill/_cli.py, src/agentscope/skill/_publisher.py, tests/skill_registry_publish_command_test.py</files>
  <read_first>src/agentscope/skill/_cli.py, src/agentscope/skill/_publisher.py, tests/skill_registry_publish_command_test.py, pyproject.toml</read_first>
  <action>Create `src/agentscope/skill/_cli.py` with a minimal `publish` command path that can be executed as a Python module for now, for example by parsing `publish <directory> --name <skill_name> --version <version> --principal <principal> [--database-url ...]`. Route that command into `publish_skill_directory(...)`, emit a clear success result including `skill_name@version`, and leave broader search/show/install subcommands for Phase 4. Run the publish command tests plus the prior publisher tests until both suites pass.</action>
  <verify>PYTHONPATH=src pytest tests/skill_registry_publisher_test.py tests/skill_registry_publish_command_test.py</verify>
  <acceptance_criteria>
    - src/agentscope/skill/_cli.py contains "publish"
    - src/agentscope/skill/_cli.py contains "principal"
    - src/agentscope/skill/_cli.py contains "database-url"
    - tests/skill_registry_publisher_test.py exits 0 under `PYTHONPATH=src pytest`
    - tests/skill_registry_publish_command_test.py exits 0 under `PYTHONPATH=src pytest`
  </acceptance_criteria>
  <done>Maintainers have a minimal publish command path and the publish workflow tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] PYTHONPATH=src pytest tests/skill_registry_publisher_test.py tests/skill_registry_publish_command_test.py
- [ ] rg -n 'publish_skill_directory|assert_can_write|identical content|different content' src/agentscope/skill/_publisher.py src/agentscope/skill/_repository.py
- [ ] rg -n 'publish|principal|database-url' src/agentscope/skill/_cli.py
</verification>

<success_criteria>

- All tasks completed
- Publish workflow tests pass
- A maintainer can publish a local skill directory by explicit name and version
- Conflicting republish is rejected and identical republish is idempotent
- Runtime loader and consumer discovery commands remain out of scope
</success_criteria>

<output>
After completion, create `.planning/phases/02-publish-workflow/02-02-SUMMARY.md`
</output>
