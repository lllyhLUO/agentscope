---
phase: 02-publish-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agentscope/skill/_publisher.py
  - tests/skill_registry_publisher_test.py
autonomous: true
requirements: [PUB-02]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: Publisher accepts a real skill directory and rejects publish attempts when top-level `SKILL.md` or required frontmatter metadata are missing"
    - "D-03/D-04: Publisher builds a stable file manifest with explicit skill name matching and deterministic version-level hashing"
    - "D-05: Scanner excludes transient local artifacts such as `__pycache__/`, `.pyc`, and `.DS_Store`"
  artifacts:
    - path: "src/agentscope/skill/_publisher.py"
      provides: "Directory scanner, metadata validator, and manifest builder"
      contains: "class SkillPublishManifest"
    - path: "tests/skill_registry_publisher_test.py"
      provides: "Validation and manifest tests for local skill directories"
      contains: "class SkillRegistryPublisherTest"
  key_links:
    - from: "src/agentscope/skill/_publisher.py"
      to: "SKILL.md"
      via: "top-level metadata parsing"
      pattern: "SKILL.md"
    - from: "src/agentscope/skill/_publisher.py"
      to: "python-frontmatter"
      via: "frontmatter parsing"
      pattern: "frontmatter"
---

<objective>
Build the local directory scanner and validator for skill publishing.

Purpose: Give the publish workflow a deterministic, testable way to validate a
local skill directory and turn it into a stable manifest before any database
write occurs.
Output: Publisher manifest types, directory validation helpers, ordered file
capture, and tests that lock validation and hashing behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-publish-workflow/02-CONTEXT.md
@docs/superpowers/specs/2026-04-28-skill-registry-design.md
@src/agentscope/skill/_models.py
@src/agentscope/skill/_repository.py
@examples/functionality/agent_skill/skill/analyzing-agentscope-library/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing tests for skill directory validation and manifest building</name>
  <files>tests/skill_registry_publisher_test.py</files>
  <read_first>tests/skill_registry_publisher_test.py, tests/skill_registry_models_test.py, tests/skill_registry_repository_test.py, examples/functionality/agent_skill/skill/analyzing-agentscope-library/SKILL.md</read_first>
  <action>Create `tests/skill_registry_publisher_test.py` with temp-directory-based tests for local skill publishing inputs. Cover: missing top-level `SKILL.md`, invalid or missing frontmatter metadata, mismatch between requested `skill_name` and frontmatter `name`, ignoring `__pycache__/`, `.pyc`, and `.DS_Store`, and deterministic manifest ordering/hash generation across repeated scans of the same directory. Run the file first in RED state before implementing the publisher helpers.</action>
  <verify>PYTHONPATH=src pytest tests/skill_registry_publisher_test.py</verify>
  <acceptance_criteria>
    - tests/skill_registry_publisher_test.py contains "class SkillRegistryPublisherTest"
    - tests/skill_registry_publisher_test.py contains "SKILL.md"
    - tests/skill_registry_publisher_test.py contains "__pycache__"
    - tests/skill_registry_publisher_test.py contains "requested skill name"
  </acceptance_criteria>
  <done>Publisher validation behavior is specified in failing tests</done>
</task>

<task type="auto">
  <name>Task 2: Implement directory validation, manifest assembly, and hashing</name>
  <files>src/agentscope/skill/_publisher.py</files>
  <read_first>src/agentscope/skill/_publisher.py, src/agentscope/skill/_repository.py, src/agentscope/skill/_models.py, .planning/phases/02-publish-workflow/02-CONTEXT.md, docs/superpowers/specs/2026-04-28-skill-registry-design.md</read_first>
  <action>Create `src/agentscope/skill/_publisher.py` with internal manifest dataclasses and helper functions that validate a local skill directory. Parse the top-level `SKILL.md` with `frontmatter`, require `name` and `description`, enforce equality between the caller-provided `skill_name` and the frontmatter `name`, recursively gather files using stable relative-path ordering, exclude `__pycache__/`, `.pyc`, and `.DS_Store`, compute per-file SHA-256 hashes, and compute a deterministic version-level `content_hash` from the ordered file manifest plus explicit version string. Keep this plan limited to local manifest preparation; do not write to PostgreSQL yet.</action>
  <verify>rg -n 'class SkillPublishManifest|content_hash|requested skill name|__pycache__|frontmatter' src/agentscope/skill/_publisher.py</verify>
  <acceptance_criteria>
    - src/agentscope/skill/_publisher.py contains "class SkillPublishManifest"
    - src/agentscope/skill/_publisher.py contains "content_hash"
    - src/agentscope/skill/_publisher.py contains "__pycache__"
    - src/agentscope/skill/_publisher.py contains "requested skill name"
    - src/agentscope/skill/_publisher.py contains "frontmatter"
  </acceptance_criteria>
  <done>Publisher can validate a skill directory and build a deterministic manifest</done>
</task>

<task type="auto">
  <name>Task 3: Turn publisher tests green and document manifest expectations in code</name>
  <files>src/agentscope/skill/_publisher.py, tests/skill_registry_publisher_test.py</files>
  <read_first>src/agentscope/skill/_publisher.py, tests/skill_registry_publisher_test.py</read_first>
  <action>Run `PYTHONPATH=src pytest tests/skill_registry_publisher_test.py`, fix any validator or manifest gaps until the suite passes, and tighten docstrings/comments only where they clarify stable manifest or hash behavior. Do not add publish-to-database behavior in this step; keep Phase 2 Wave 1 strictly on local directory preparation.</action>
  <verify>PYTHONPATH=src pytest tests/skill_registry_publisher_test.py</verify>
  <acceptance_criteria>
    - tests/skill_registry_publisher_test.py exits 0 under `PYTHONPATH=src pytest`
    - src/agentscope/skill/_publisher.py contains docstrings for the public manifest helpers introduced in this plan
  </acceptance_criteria>
  <done>Local publish manifest behavior is tested and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] PYTHONPATH=src pytest tests/skill_registry_publisher_test.py
- [ ] rg -n 'class SkillPublishManifest|content_hash|__pycache__|frontmatter' src/agentscope/skill/_publisher.py
- [ ] rg -n 'SKILL.md|requested skill name|__pycache__' tests/skill_registry_publisher_test.py
</verification>

<success_criteria>

- All tasks completed
- Publisher validation tests pass
- The local manifest builder is deterministic and excludes transient files
- No PostgreSQL writes or command-path behavior are added in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/02-publish-workflow/02-01-SUMMARY.md`
</output>
