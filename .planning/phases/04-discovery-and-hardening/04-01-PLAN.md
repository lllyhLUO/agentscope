---
phase: 04-discovery-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/agentscope/skill/_cli.py
  - tests/skill_registry_cli_test.py
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03]
user_setup:
  - service: postgresql
    why: "Real search/show/install usage requires a readable registry database URL"
    env_vars:
      - name: AGENTSCOPE_SKILL_REGISTRY_DATABASE_URL
        source: "Developer-provided read-capable PostgreSQL URL for the shared skill registry"
must_haves:
  truths:
    - "D-01/D-02: Users can search registry skills from the existing skill CLI without needing publish permissions"
    - "D-03/D-05: Users can inspect and install only explicit `skill_name@version` refs"
    - "D-04/D-06: Install prewarms the managed cache and reports the hydrated path without claiming the cache is the source of truth, and terminal output stays understandable in plain text"
  artifacts:
    - path: "src/agentscope/skill/_cli.py"
      provides: "Search/show/install command paths"
      contains: "search"
    - path: "tests/skill_registry_cli_test.py"
      provides: "CLI coverage for search/show/install behavior"
      contains: "class SkillRegistryCliTest"
    - path: "pyproject.toml"
      provides: "User-facing skill CLI entrypoint if phase chooses to expose one"
      contains: "agentscope-skill"
  key_links:
    - from: "src/agentscope/skill/_cli.py"
      to: "src/agentscope/skill/_repository.py"
      via: "search and show command dispatch"
      pattern: "search_skills"
    - from: "src/agentscope/skill/_cli.py"
      to: "src/agentscope/skill/_cache.py"
      via: "install command prewarm"
      pattern: "SkillRuntimeCache"
---

<objective>
Extend the skill CLI with end-user discovery and install commands.

Purpose: Make the shared registry usable from the terminal for discovery,
inspection, and cache prewarming before full documentation is added.
Output: `search`, `show`, and `install` command paths, optional CLI entrypoint
exposure, and CLI-focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-discovery-and-hardening/04-CONTEXT.md
@.planning/phases/03-runtime-loader/03-01-SUMMARY.md
@.planning/phases/03-runtime-loader/03-02-SUMMARY.md
@src/agentscope/skill/_cli.py
@src/agentscope/skill/_repository.py
@src/agentscope/skill/_cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing CLI tests for search, show, and install</name>
  <files>tests/skill_registry_cli_test.py</files>
  <read_first>tests/skill_registry_cli_test.py, tests/skill_registry_publish_command_test.py, tests/skill_registry_cache_test.py, src/agentscope/skill/_cli.py, .planning/phases/04-discovery-and-hardening/04-CONTEXT.md</read_first>
  <action>Create `tests/skill_registry_cli_test.py` with isolated CLI tests that cover: `search` dispatch to repository-backed lookup, `show` for explicit `skill_name@version` with file listing, rejection of `show` or `install` when `@version` is missing, and `install` delegating to the managed runtime cache while reporting the hydrated path. If you decide to expose a real package script entrypoint, add a parser/entrypoint assertion for that too. Run the file first in RED state.</action>
  <verify>PYTHONPATH=src pytest tests/skill_registry_cli_test.py</verify>
  <acceptance_criteria>
    - tests/skill_registry_cli_test.py contains "class SkillRegistryCliTest"
    - tests/skill_registry_cli_test.py contains "search"
    - tests/skill_registry_cli_test.py contains "show"
    - tests/skill_registry_cli_test.py contains "install"
    - tests/skill_registry_cli_test.py contains "@version"
  </acceptance_criteria>
  <done>CLI discovery behavior is specified in failing tests</done>
</task>

<task type="auto">
  <name>Task 2: Implement search/show/install command paths and expose the chosen CLI entrypoint</name>
  <files>src/agentscope/skill/_cli.py, pyproject.toml</files>
  <read_first>src/agentscope/skill/_cli.py, src/agentscope/skill/_repository.py, src/agentscope/skill/_cache.py, src/agentscope/skill/_loader.py, pyproject.toml, .planning/phases/04-discovery-and-hardening/04-CONTEXT.md</read_first>
  <action>Extend `_cli.py` to support `search`, `show`, and `install` subcommands alongside `publish`. `search` should accept a query string and optional limit, call repository search, and print readable results. `show` must require explicit `skill_name@version`, fetch version metadata plus file list, and print a human-readable summary. `install` must require explicit `skill_name@version`, hydrate the managed runtime cache via `SkillRuntimeCache`, and print the hydrated path. If you decide the workflow should be directly invokable after package install, add a `[project.scripts]` entry such as `agentscope-skill = "agentscope.skill._cli:main"` in `pyproject.toml`; otherwise keep invocation documented through `python -m agentscope.skill._cli` and make tests assert that path instead.</action>
  <verify>rg -n 'search|show|install|agentscope-skill|SkillRuntimeCache|search_skills|get_skill_version|list_skill_files' src/agentscope/skill/_cli.py pyproject.toml</verify>
  <acceptance_criteria>
    - src/agentscope/skill/_cli.py contains "search"
    - src/agentscope/skill/_cli.py contains "show"
    - src/agentscope/skill/_cli.py contains "install"
    - src/agentscope/skill/_cli.py contains "SkillRuntimeCache"
    - src/agentscope/skill/_cli.py contains "get_skill_version"
    - src/agentscope/skill/_cli.py contains "list_skill_files"
  </acceptance_criteria>
  <done>The skill CLI exposes discovery and install flows for end users</done>
</task>

<task type="auto">
  <name>Task 3: Turn CLI tests green and keep install semantics cache-only</name>
  <files>src/agentscope/skill/_cli.py, tests/skill_registry_cli_test.py</files>
  <read_first>src/agentscope/skill/_cli.py, tests/skill_registry_cli_test.py</read_first>
  <action>Run `PYTHONPATH=src pytest tests/skill_registry_cli_test.py`, fix any command routing or output contract gaps until the suite passes, and ensure `install` only hydrates cache/prewarm state rather than auto-registering into Toolkit or mutating registry data.</action>
  <verify>PYTHONPATH=src pytest tests/skill_registry_cli_test.py</verify>
  <acceptance_criteria>
    - tests/skill_registry_cli_test.py exits 0 under `PYTHONPATH=src pytest`
    - CLI output for `install` includes the hydrated path or clear equivalent success text
  </acceptance_criteria>
  <done>Discovery/install CLI behavior is tested and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] PYTHONPATH=src pytest tests/skill_registry_cli_test.py
- [ ] rg -n 'search|show|install|SkillRuntimeCache|search_skills|get_skill_version|list_skill_files' src/agentscope/skill/_cli.py
- [ ] If a console script is added: pyproject.toml contains '[project.scripts]' and 'agentscope-skill'
</verification>

<success_criteria>

- All tasks completed
- Discovery/install CLI tests pass
- Users can search, inspect, and install explicit-version skills from the terminal
- Install remains a cache prewarm step, not a runtime registration side effect
</success_criteria>

<output>
After completion, create `.planning/phases/04-discovery-and-hardening/04-01-SUMMARY.md`
</output>
