---
phase: 01-registry-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agentscope/skill/__init__.py
  - src/agentscope/skill/_models.py
  - tests/skill_registry_models_test.py
autonomous: true
requirements: [REG-02, REG-03, REG-04]
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: Registry schema defines globally unique skill names and explicit version rows in the `skills` and `skill_versions` tables"
    - "D-03: Registry schema stores immutable version rows and per-file records for each published skill version"
    - "D-05: Schema constraints and package layout live under `src/agentscope/skill/` and prevent duplicate `(skill_id, version)` pairs and duplicate `(skill_version_id, path)` pairs"
  artifacts:
    - path: "src/agentscope/skill/_models.py"
      provides: "Declarative SQLAlchemy registry models and constraints"
      contains: "class _SkillTable"
    - path: "tests/skill_registry_models_test.py"
      provides: "Model-level coverage for table names and constraints"
      contains: "class SkillRegistryModelsTest"
  key_links:
    - from: "src/agentscope/skill/_models.py"
      to: "skills"
      via: "__tablename__"
      pattern: "__tablename__ = \"skills\""
    - from: "src/agentscope/skill/_models.py"
      to: "skill_versions"
      via: "foreign key"
      pattern: "ForeignKey\\(\"skills.id\"\\)"
    - from: "src/agentscope/skill/_models.py"
      to: "skill_files"
      via: "foreign key"
      pattern: "ForeignKey\\(\"skill_versions.id\"\\)"
---

<objective>
Create the persistent data model for the AgentScope skill registry package.

Purpose: Give later publish and loader phases a stable schema for skill
identity, versioning, file storage, and maintainer ownership.
Output: A new `agentscope.skill` package shell, declarative SQLAlchemy models,
and model-focused tests that lock table names and uniqueness constraints.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-registry-foundation/01-CONTEXT.md
@docs/superpowers/specs/2026-04-28-skill-registry-design.md
@src/agentscope/memory/_working_memory/_sqlalchemy_memory.py
@src/agentscope/memory/_working_memory/__init__.py
@src/agentscope/session/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the `agentscope.skill` package and model base</name>
  <files>src/agentscope/skill/__init__.py, src/agentscope/skill/_models.py</files>
  <read_first>src/agentscope/skill/__init__.py, src/agentscope/skill/_models.py, src/agentscope/memory/_working_memory/__init__.py, src/agentscope/session/__init__.py, src/agentscope/memory/_working_memory/_sqlalchemy_memory.py</read_first>
  <action>Create `src/agentscope/skill/` if missing. Add `src/agentscope/skill/_models.py` with module docstring, `declarative_base()` setup named `_SkillRegistryBase`, and internal SQLAlchemy model class shells named `_SkillTable`, `_SkillVersionTable`, `_SkillFileTable`, and `_SkillMaintainerTable`. Add `src/agentscope/skill/__init__.py` with package docstring and package exports limited to registry foundation symbols created in this phase; do not expose raw SQLAlchemy table classes as public top-level API if no downstream caller needs them yet.</action>
  <verify>test -f src/agentscope/skill/_models.py && test -f src/agentscope/skill/__init__.py</verify>
  <acceptance_criteria>
    - src/agentscope/skill/_models.py contains "class _SkillTable"
    - src/agentscope/skill/_models.py contains "class _SkillVersionTable"
    - src/agentscope/skill/_models.py contains "class _SkillFileTable"
    - src/agentscope/skill/_models.py contains "class _SkillMaintainerTable"
    - src/agentscope/skill/__init__.py contains "The skill module"
  </acceptance_criteria>
  <done>`agentscope.skill` exists and contains the registry model module plus package surface</done>
</task>

<task type="auto">
  <name>Task 2: Encode the registry table structure and uniqueness constraints</name>
  <files>src/agentscope/skill/_models.py</files>
  <read_first>src/agentscope/skill/_models.py, src/agentscope/memory/_working_memory/_sqlalchemy_memory.py, .planning/phases/01-registry-foundation/01-CONTEXT.md, docs/superpowers/specs/2026-04-28-skill-registry-design.md</read_first>
  <action>Implement the four canonical tables exactly as approved in the design doc: `skills`, `skill_versions`, `skill_files`, and `skill_maintainers`. Use SQLAlchemy columns for `name`, `version`, `latest_version`, `status`, `description`, `metadata_json`, `content_hash`, `content_text`, `content_bytes`, `sha256`, `size`, `principal`, `role`, timestamps, and creator/publisher identity fields. Add a unique constraint on `skills.name`, a composite unique constraint on `skill_versions(skill_id, version)`, and a composite unique constraint on `skill_files(skill_version_id, path)`. Add foreign keys from versions to skills, files to versions, and maintainers to skills. Model per-file text/binary storage with both `content_text` and `content_bytes` columns so later phases can persist `SKILL.md`, scripts, and binary assets without schema changes.</action>
  <verify>rg -n '__tablename__ = \"skills\"|__tablename__ = \"skill_versions\"|__tablename__ = \"skill_files\"|__tablename__ = \"skill_maintainers\"|UniqueConstraint\\(\"skill_id\", \"version\"|UniqueConstraint\\(\"skill_version_id\", \"path\"' src/agentscope/skill/_models.py</verify>
  <acceptance_criteria>
    - src/agentscope/skill/_models.py contains "__tablename__ = \"skills\""
    - src/agentscope/skill/_models.py contains "__tablename__ = \"skill_versions\""
    - src/agentscope/skill/_models.py contains "__tablename__ = \"skill_files\""
    - src/agentscope/skill/_models.py contains "__tablename__ = \"skill_maintainers\""
    - src/agentscope/skill/_models.py contains "UniqueConstraint(\"skill_id\", \"version\""
    - src/agentscope/skill/_models.py contains "UniqueConstraint(\"skill_version_id\", \"path\""
    - src/agentscope/skill/_models.py contains "content_text"
    - src/agentscope/skill/_models.py contains "content_bytes"
  </acceptance_criteria>
  <done>The registry schema matches the approved data model and constraint set</done>
</task>

<task type="auto">
  <name>Task 3: Add model-focused tests that lock the schema contract</name>
  <files>tests/skill_registry_models_test.py</files>
  <read_first>tests/skill_registry_models_test.py, tests/toolkit_basic_test.py, tests/tablestore_memory_test.py, src/agentscope/skill/_models.py</read_first>
  <action>Create `tests/skill_registry_models_test.py` using `IsolatedAsyncioTestCase` or standard `unittest.TestCase` as appropriate for metadata inspection. Assert that metadata contains exactly the four planned table names, that `skills.name` is unique, that the version and file composite uniqueness constraints exist, and that version/file foreign keys point to the expected parent tables. Keep this suite model-focused: no live PostgreSQL dependency, no repository behavior, and no publish workflow logic in this file.</action>
  <verify>pytest tests/skill_registry_models_test.py</verify>
  <acceptance_criteria>
    - tests/skill_registry_models_test.py contains "class SkillRegistryModelsTest"
    - tests/skill_registry_models_test.py contains "skills"
    - tests/skill_registry_models_test.py contains "skill_versions"
    - tests/skill_registry_models_test.py contains "skill_files"
    - tests/skill_registry_models_test.py contains "skill_maintainers"
    - tests/skill_registry_models_test.py exits 0 under pytest
  </acceptance_criteria>
  <done>Schema-level contract tests exist and pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest tests/skill_registry_models_test.py
- [ ] rg -n 'UniqueConstraint\\(\"skill_id\", \"version\"|UniqueConstraint\\(\"skill_version_id\", \"path\"' src/agentscope/skill/_models.py
- [ ] rg -n '__tablename__ = \"skills\"|__tablename__ = \"skill_versions\"|__tablename__ = \"skill_files\"|__tablename__ = \"skill_maintainers\"' src/agentscope/skill/_models.py
</verification>

<success_criteria>

- All tasks completed
- Model tests pass
- Schema names and uniqueness constraints match the approved design
- No runtime loader, publish CLI, or Toolkit wiring is introduced in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-registry-foundation/01-01-SUMMARY.md`
</output>
