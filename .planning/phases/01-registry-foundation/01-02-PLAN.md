---
phase: 01-registry-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/agentscope/skill/__init__.py
  - src/agentscope/skill/_repository.py
  - pyproject.toml
  - tests/skill_registry_repository_test.py
autonomous: true
requirements: [REG-01, ACC-01, ACC-02]
user_setup:
  - service: postgresql
    why: "Optional live integration verification for the registry repository needs a reachable PostgreSQL database"
    env_vars:
      - name: AGENTSCOPE_SKILL_REGISTRY_DATABASE_URL
        source: "Developer-provided connection string for the shared registry database"
      - name: AGENTSCOPE_SKILL_REGISTRY_TEST_DATABASE_URL
        source: "Developer-provided isolated PostgreSQL database for integration checks"
must_haves:
  truths:
    - "D-06: Repository can initialize the registry schema from a configured async SQLAlchemy engine or session and supports `AGENTSCOPE_SKILL_REGISTRY_DATABASE_URL`"
    - "D-04: Repository exposes public-read queries for exact skill/version lookup and search"
    - "D-04/D-06: Repository exposes explicit maintainer-gated write checks instead of assuming all callers can publish"
  artifacts:
    - path: "src/agentscope/skill/_repository.py"
      provides: "Async registry repository with connection/config and access methods"
      contains: "class SkillRegistryRepository"
    - path: "tests/skill_registry_repository_test.py"
      provides: "Repository coverage for schema initialization and access checks"
      contains: "class SkillRegistryRepositoryTest"
    - path: "pyproject.toml"
      provides: "Optional dependency path for PostgreSQL async driver"
      contains: "skill_registry = ["
  key_links:
    - from: "src/agentscope/skill/_repository.py"
      to: "src/agentscope/skill/_models.py"
      via: "metadata create_all and ORM queries"
      pattern: "_SkillRegistryBase\\.metadata"
    - from: "src/agentscope/skill/_repository.py"
      to: "AGENTSCOPE_SKILL_REGISTRY_DATABASE_URL"
      via: "environment variable lookup"
      pattern: "AGENTSCOPE_SKILL_REGISTRY_DATABASE_URL"
    - from: "src/agentscope/skill/_repository.py"
      to: "_SkillMaintainerTable"
      via: "maintainer access check"
      pattern: "_SkillMaintainerTable"
---

<objective>
Implement the repository and connection boundary for the AgentScope skill
registry foundation.

Purpose: Make the schema usable from real code by adding async connection
plumbing, public-read repository methods, and explicit maintainer write checks.
Output: A repository module, package export updates, dependency wiring for the
async PostgreSQL driver path, and repository-focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-registry-foundation/01-CONTEXT.md
@docs/superpowers/specs/2026-04-28-skill-registry-design.md
@src/agentscope/skill/_models.py
@src/agentscope/memory/_working_memory/_sqlalchemy_memory.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add repository connection and schema initialization primitives</name>
  <files>src/agentscope/skill/_repository.py, pyproject.toml</files>
  <read_first>src/agentscope/skill/_repository.py, src/agentscope/memory/_working_memory/_sqlalchemy_memory.py, pyproject.toml, .planning/phases/01-registry-foundation/01-CONTEXT.md</read_first>
  <action>Create `src/agentscope/skill/_repository.py` with a public `SkillRegistryRepository` class. Mirror the existing async engine/session handling style from `AsyncSQLAlchemyMemory`: accept `AsyncEngine` or `AsyncSession` injection, lazily create internal sessions when given an engine, and provide a `from_env()` constructor that reads `AGENTSCOPE_SKILL_REGISTRY_DATABASE_URL`. Add `init_schema()` that calls `_SkillRegistryBase.metadata.create_all(...)` against the async engine. Update `pyproject.toml` with a new optional dependency group named `skill_registry` that includes `asyncpg`, and make repository error messages tell users to install `agentscope[skill_registry]` when PostgreSQL driver resolution fails.</action>
  <verify>rg -n 'class SkillRegistryRepository|from_env|AGENTSCOPE_SKILL_REGISTRY_DATABASE_URL|init_schema|skill_registry = \\[' src/agentscope/skill/_repository.py pyproject.toml</verify>
  <acceptance_criteria>
    - src/agentscope/skill/_repository.py contains "class SkillRegistryRepository"
    - src/agentscope/skill/_repository.py contains "AGENTSCOPE_SKILL_REGISTRY_DATABASE_URL"
    - src/agentscope/skill/_repository.py contains "async def init_schema"
    - pyproject.toml contains "skill_registry = ["
    - pyproject.toml contains "asyncpg"
  </acceptance_criteria>
  <done>Repository can be constructed from env or injected async SQLAlchemy objects and can initialize schema</done>
</task>

<task type="auto">
  <name>Task 2: Implement public-read queries and maintainer-gated write checks</name>
  <files>src/agentscope/skill/_repository.py</files>
  <read_first>src/agentscope/skill/_repository.py, src/agentscope/skill/_models.py, docs/superpowers/specs/2026-04-28-skill-registry-design.md, .planning/REQUIREMENTS.md</read_first>
  <action>Implement repository methods needed by later publish and loader phases without adding the full publish workflow yet. Add exact-read methods for `get_skill(name)`, `get_skill_version(name, version)`, `list_skill_versions(name)`, `search_skills(query, limit)`, and `list_skill_files(name, version)`. Add access helpers `is_skill_maintainer(skill_name, principal)` and `assert_can_write(skill_name, principal)` so public reads and maintainer writes are distinct concepts at the repository boundary. Return structured Python data instead of leaking raw ORM rows widely beyond the repository class.</action>
  <verify>rg -n 'def get_skill|def get_skill_version|def list_skill_versions|def search_skills|def list_skill_files|def is_skill_maintainer|def assert_can_write' src/agentscope/skill/_repository.py</verify>
  <acceptance_criteria>
    - src/agentscope/skill/_repository.py contains "def get_skill("
    - src/agentscope/skill/_repository.py contains "def get_skill_version("
    - src/agentscope/skill/_repository.py contains "def list_skill_versions("
    - src/agentscope/skill/_repository.py contains "def search_skills("
    - src/agentscope/skill/_repository.py contains "def list_skill_files("
    - src/agentscope/skill/_repository.py contains "def is_skill_maintainer("
    - src/agentscope/skill/_repository.py contains "def assert_can_write("
  </acceptance_criteria>
  <done>Repository exposes the minimum Phase 1 read and access-check surface</done>
</task>

<task type="auto">
  <name>Task 3: Add repository tests and finalize package exports</name>
  <files>src/agentscope/skill/__init__.py, tests/skill_registry_repository_test.py</files>
  <read_first>src/agentscope/skill/__init__.py, tests/skill_registry_repository_test.py, tests/toolkit_basic_test.py, tests/session_test.py, src/agentscope/skill/_repository.py</read_first>
  <action>Update `src/agentscope/skill/__init__.py` to expose `SkillRegistryRepository` as the public registry foundation symbol for this phase. Add `tests/skill_registry_repository_test.py` with repository-focused coverage using injected async SQLite sessions for unit tests and optional env-gated PostgreSQL checks for `AGENTSCOPE_SKILL_REGISTRY_TEST_DATABASE_URL`. Test schema initialization, exact version lookup, search behavior, and maintainer access checks. Keep publish directory scanning, runtime cache, and Toolkit integration out of this test file because those belong to later phases.</action>
  <verify>pytest tests/skill_registry_repository_test.py</verify>
  <acceptance_criteria>
    - src/agentscope/skill/__init__.py contains "SkillRegistryRepository"
    - tests/skill_registry_repository_test.py contains "class SkillRegistryRepositoryTest"
    - tests/skill_registry_repository_test.py contains "AGENTSCOPE_SKILL_REGISTRY_TEST_DATABASE_URL"
    - tests/skill_registry_repository_test.py exits 0 under pytest
  </acceptance_criteria>
  <done>Repository coverage exists and package exports are ready for later phases</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pytest tests/skill_registry_repository_test.py
- [ ] rg -n 'class SkillRegistryRepository|AGENTSCOPE_SKILL_REGISTRY_DATABASE_URL|def assert_can_write' src/agentscope/skill/_repository.py
- [ ] rg -n 'skill_registry = \\[|asyncpg' pyproject.toml
</verification>

<success_criteria>

- All tasks completed
- Repository tests pass
- Schema initialization works from repository code
- Public read and maintainer write are explicit repository concepts
- No publish CLI, runtime cache, or Toolkit loading behavior is implemented in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-registry-foundation/01-02-SUMMARY.md`
</output>
