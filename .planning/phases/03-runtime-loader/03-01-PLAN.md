---
phase: 03-runtime-loader
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agentscope/skill/__init__.py
  - src/agentscope/skill/_cache.py
  - tests/skill_registry_cache_test.py
autonomous: true
requirements: [RUN-02, ACC-03]
user_setup: []
must_haves:
  truths:
    - "D-02/D-03: Runtime cache is not treated as the source of truth and is keyed by explicit skill version plus content hash"
    - "D-04/D-05: Cache hydration materializes a real directory tree outside the repo working tree with top-level `SKILL.md` and all published files"
    - "Cache refresh removes stale cached versions when database content hash changes"
  artifacts:
    - path: "src/agentscope/skill/_cache.py"
      provides: "Managed runtime cache for published skill versions"
      contains: "class SkillRuntimeCache"
    - path: "tests/skill_registry_cache_test.py"
      provides: "Cache hydration, reuse, and invalidation coverage"
      contains: "class SkillRegistryCacheTest"
  key_links:
    - from: "src/agentscope/skill/_cache.py"
      to: "src/agentscope/skill/_repository.py"
      via: "exact version file retrieval and hash validation"
      pattern: "list_skill_files"
    - from: "src/agentscope/skill/_cache.py"
      to: "SKILL.md"
      via: "cache hydration"
      pattern: "SKILL.md"
---

<objective>
Implement the managed runtime cache for registry-backed skills.

Purpose: Materialize explicit registry versions into real local directories so
later loader code can reuse the existing directory-based skill registration path.
Output: Cache path resolver, explicit reference parser, cache hydration and
staleness invalidation behavior, plus cache-focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-runtime-loader/03-CONTEXT.md
@.planning/phases/02-publish-workflow/02-01-SUMMARY.md
@.planning/phases/02-publish-workflow/02-02-SUMMARY.md
@docs/superpowers/specs/2026-04-28-skill-registry-design.md
@src/agentscope/embedding/_file_cache.py
@src/agentscope/skill/_repository.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing tests for explicit skill ref parsing and cache hydration</name>
  <files>tests/skill_registry_cache_test.py</files>
  <read_first>tests/skill_registry_cache_test.py, tests/embedding_cache_test.py, tests/skill_registry_publish_command_test.py, src/agentscope/embedding/_file_cache.py, .planning/phases/03-runtime-loader/03-CONTEXT.md</read_first>
  <action>Create `tests/skill_registry_cache_test.py` with temp-directory-based tests for runtime cache behavior. Cover: parsing only valid `skill_name@version` refs, rejecting missing-version refs, materializing `SKILL.md` and additional files into a managed cache directory, reusing an existing cache directory when content hash matches, and rebuilding the cache when the repository reports a different content hash for the same `skill_name@version`.</action>
  <verify>PYTHONPATH=src pytest tests/skill_registry_cache_test.py</verify>
  <acceptance_criteria>
    - tests/skill_registry_cache_test.py contains "class SkillRegistryCacheTest"
    - tests/skill_registry_cache_test.py contains "skill_name@version"
    - tests/skill_registry_cache_test.py contains "SKILL.md"
    - tests/skill_registry_cache_test.py contains "content hash"
  </acceptance_criteria>
  <done>Cache behavior is specified in failing tests</done>
</task>

<task type="auto">
  <name>Task 2: Implement managed cache pathing, explicit ref parsing, and hydration</name>
  <files>src/agentscope/skill/_cache.py, src/agentscope/skill/__init__.py</files>
  <read_first>src/agentscope/skill/_cache.py, src/agentscope/skill/__init__.py, src/agentscope/embedding/_file_cache.py, src/agentscope/skill/_repository.py, .planning/phases/03-runtime-loader/03-CONTEXT.md</read_first>
  <action>Create `src/agentscope/skill/_cache.py` with a public `SkillRuntimeCache` class and any small helper dataclasses needed. Implement strict parsing for `skill_name@version`, choose a default cache root outside the repo working tree (for example under the user's cache home), and hydrate registry files into a real directory tree keyed by skill name, version, and content hash. When an existing cache directory's stored content hash matches the repository result, reuse it; when it differs, delete the stale directory and rebuild from repository file records. Update `src/agentscope/skill/__init__.py` to export the cache entrypoint(s) needed by Phase 3.</action>
  <verify>rg -n 'class SkillRuntimeCache|skill_name@version|content hash|SKILL.md' src/agentscope/skill/_cache.py src/agentscope/skill/__init__.py</verify>
  <acceptance_criteria>
    - src/agentscope/skill/_cache.py contains "class SkillRuntimeCache"
    - src/agentscope/skill/_cache.py contains "skill_name@version"
    - src/agentscope/skill/_cache.py contains "content hash"
    - src/agentscope/skill/_cache.py contains "SKILL.md"
    - src/agentscope/skill/__init__.py contains "SkillRuntimeCache"
  </acceptance_criteria>
  <done>Managed runtime cache can hydrate and refresh explicit skill versions</done>
</task>

<task type="auto">
  <name>Task 3: Turn cache tests green and keep cache semantics internal-only</name>
  <files>src/agentscope/skill/_cache.py, tests/skill_registry_cache_test.py</files>
  <read_first>src/agentscope/skill/_cache.py, tests/skill_registry_cache_test.py</read_first>
  <action>Run `PYTHONPATH=src pytest tests/skill_registry_cache_test.py`, fix any parsing or hydration gaps until the suite passes, and ensure the cache implementation never claims to be the source of truth. Keep this plan limited to cache materialization and validation; do not modify `Toolkit` yet.</action>
  <verify>PYTHONPATH=src pytest tests/skill_registry_cache_test.py</verify>
  <acceptance_criteria>
    - tests/skill_registry_cache_test.py exits 0 under `PYTHONPATH=src pytest`
    - src/agentscope/skill/_cache.py contains docstrings for the public cache helpers introduced in this plan
  </acceptance_criteria>
  <done>Runtime cache behavior is tested and passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] PYTHONPATH=src pytest tests/skill_registry_cache_test.py
- [ ] rg -n 'class SkillRuntimeCache|skill_name@version|content hash|SKILL.md' src/agentscope/skill/_cache.py
- [ ] rg -n 'skill_name@version|SKILL.md|content hash' tests/skill_registry_cache_test.py
</verification>

<success_criteria>

- All tasks completed
- Cache tests pass
- Runtime cache creates and refreshes real directory trees from explicit skill refs
- No `Toolkit` API changes are introduced in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-loader/03-01-SUMMARY.md`
</output>
