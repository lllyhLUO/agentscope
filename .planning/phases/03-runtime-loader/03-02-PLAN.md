---
phase: 03-runtime-loader
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/agentscope/skill/__init__.py
  - src/agentscope/skill/_loader.py
  - src/agentscope/tool/_toolkit.py
  - tests/skill_registry_loader_test.py
autonomous: true
requirements: [RUN-01, RUN-03]
user_setup:
  - service: postgresql
    why: "Live runtime loading requires a readable registry database URL"
    env_vars:
      - name: AGENTSCOPE_SKILL_REGISTRY_DATABASE_URL
        source: "Developer-provided read-capable PostgreSQL URL for the shared skill registry"
must_haves:
  truths:
    - "D-01/D-08: Toolkit runtime loading rejects invalid refs that omit explicit version"
    - "D-06/D-07: Toolkit exposes a registry-specific loading API that reuses `register_agent_skill()` after cache hydration"
    - "D-09: Loader and Toolkit integration support injected repository/cache collaborators for tests"
  artifacts:
    - path: "src/agentscope/skill/_loader.py"
      provides: "Registry loader that bridges repository/cache into Toolkit"
      contains: "class SkillRegistryLoader"
    - path: "src/agentscope/tool/_toolkit.py"
      provides: "Registry-aware Toolkit method"
      contains: "register_registry_skill"
    - path: "tests/skill_registry_loader_test.py"
      provides: "Loader and Toolkit integration coverage"
      contains: "class SkillRegistryLoaderTest"
  key_links:
    - from: "src/agentscope/skill/_loader.py"
      to: "src/agentscope/skill/_cache.py"
      via: "managed cache hydration"
      pattern: "SkillRuntimeCache"
    - from: "src/agentscope/tool/_toolkit.py"
      to: "register_agent_skill"
      via: "delegation after hydration"
      pattern: "register_agent_skill"
---

<objective>
Integrate registry-backed runtime loading into Toolkit through a new loader API.

Purpose: Let users load a published skill by explicit `skill_name@version`
without changing the existing local skill registration contract.
Output: Loader helpers, a new Toolkit registry API, and tests proving that cache
hydration flows into `register_agent_skill()` correctly.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-runtime-loader/03-CONTEXT.md
@.planning/phases/02-publish-workflow/02-02-SUMMARY.md
@src/agentscope/tool/_toolkit.py
@src/agentscope/skill/_cache.py
@src/agentscope/skill/_repository.py
@docs/superpowers/specs/2026-04-28-skill-registry-design.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing tests for loader behavior and Toolkit integration</name>
  <files>tests/skill_registry_loader_test.py</files>
  <read_first>tests/skill_registry_loader_test.py, tests/toolkit_basic_test.py, tests/skill_registry_cache_test.py, src/agentscope/tool/_toolkit.py, .planning/phases/03-runtime-loader/03-CONTEXT.md</read_first>
  <action>Create `tests/skill_registry_loader_test.py` with loader-focused tests. Cover: invalid refs without `@version` raise before registration, loader asks the runtime cache for a hydrated path, the new Toolkit API delegates into `register_agent_skill()` with that hydrated path, and injected repository/cache collaborators can be supplied in tests without needing a live PostgreSQL instance.</action>
  <verify>PYTHONPATH=src pytest tests/skill_registry_loader_test.py</verify>
  <acceptance_criteria>
    - tests/skill_registry_loader_test.py contains "class SkillRegistryLoaderTest"
    - tests/skill_registry_loader_test.py contains "@version"
    - tests/skill_registry_loader_test.py contains "register_agent_skill"
    - tests/skill_registry_loader_test.py contains "register_registry_skill"
  </acceptance_criteria>
  <done>Loader and Toolkit behavior is specified in failing tests</done>
</task>

<task type="auto">
  <name>Task 2: Implement the registry loader and new Toolkit API</name>
  <files>src/agentscope/skill/_loader.py, src/agentscope/skill/__init__.py, src/agentscope/tool/_toolkit.py</files>
  <read_first>src/agentscope/skill/_loader.py, src/agentscope/skill/__init__.py, src/agentscope/tool/_toolkit.py, src/agentscope/skill/_cache.py, src/agentscope/skill/_repository.py, .planning/phases/03-runtime-loader/03-CONTEXT.md</read_first>
  <action>Create `src/agentscope/skill/_loader.py` with a `SkillRegistryLoader` class that accepts injected repository and cache collaborators or builds them from env-backed defaults. Implement explicit skill-ref validation, call into `SkillRuntimeCache` to hydrate the requested version, and return the hydrated local path. Update `src/agentscope/tool/_toolkit.py` with a new `register_registry_skill(skill_ref, registry_loader=None)` method that resolves the path through the loader and then calls the existing `register_agent_skill()` method unchanged. Export the loader entrypoint through `src/agentscope/skill/__init__.py`.</action>
  <verify>rg -n 'class SkillRegistryLoader|register_registry_skill|register_agent_skill|skill_name@version' src/agentscope/skill/_loader.py src/agentscope/skill/__init__.py src/agentscope/tool/_toolkit.py</verify>
  <acceptance_criteria>
    - src/agentscope/skill/_loader.py contains "class SkillRegistryLoader"
    - src/agentscope/tool/_toolkit.py contains "def register_registry_skill"
    - src/agentscope/tool/_toolkit.py contains "register_agent_skill"
    - src/agentscope/skill/__init__.py contains "SkillRegistryLoader"
  </acceptance_criteria>
  <done>Toolkit can resolve registry-backed skills through the loader without changing the local registration path</done>
</task>

<task type="auto">
  <name>Task 3: Turn loader tests green and confirm no regression in existing skill registration</name>
  <files>src/agentscope/skill/_loader.py, src/agentscope/tool/_toolkit.py, tests/skill_registry_loader_test.py</files>
  <read_first>src/agentscope/skill/_loader.py, src/agentscope/tool/_toolkit.py, tests/skill_registry_loader_test.py, tests/toolkit_basic_test.py</read_first>
  <action>Run `PYTHONPATH=src pytest tests/skill_registry_loader_test.py`, fix integration gaps until it passes, then re-run a focused existing Toolkit test that proves local skill registration still behaves normally. Keep this plan scoped to runtime loading only; search/show/install UX remains Phase 4 work.</action>
  <verify>PYTHONPATH=src pytest tests/skill_registry_loader_test.py tests/toolkit_basic_test.py -k 'duplicate_tool_registration or register_registry_skill'</verify>
  <acceptance_criteria>
    - tests/skill_registry_loader_test.py exits 0 under `PYTHONPATH=src pytest`
    - focused toolkit regression checks exit 0 under `PYTHONPATH=src pytest`
  </acceptance_criteria>
  <done>Registry-aware Toolkit loading works and does not regress local skill registration</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] PYTHONPATH=src pytest tests/skill_registry_loader_test.py
- [ ] PYTHONPATH=src pytest tests/toolkit_basic_test.py -k 'duplicate_tool_registration or register_registry_skill'
- [ ] rg -n 'class SkillRegistryLoader|register_registry_skill|register_agent_skill|skill_name@version' src/agentscope/skill/_loader.py src/agentscope/tool/_toolkit.py
</verification>

<success_criteria>

- All tasks completed
- Loader tests pass
- Toolkit can load a registry skill only by explicit versioned ref
- Local `register_agent_skill(path)` behavior remains intact
</success_criteria>

<output>
After completion, create `.planning/phases/03-runtime-loader/03-02-SUMMARY.md`
</output>
